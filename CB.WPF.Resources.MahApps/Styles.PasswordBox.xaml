<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/styles/controls.passwordbox.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style x:Key="PasswordBoxStyle" TargetType="PasswordBox" BasedOn="{StaticResource MetroPasswordBox}">
        <Setter Property="Margin" Value="{DynamicResource NormalMargion}" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>

    <Style TargetType="PasswordBox" BasedOn="{StaticResource PasswordBoxStyle}" />

    <Style x:Key="PasswordBoxStyle.ButtonRevealed" TargetType="PasswordBox"
           BasedOn="{StaticResource MetroButtonRevealedPasswordBox}">
        <Setter Property="Margin" Value="{DynamicResource NormalMargion}" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>

    <Style x:Key="PasswordBoxStyle.Win8" TargetType="PasswordBox"
           BasedOn="{StaticResource Win8MetroPasswordBox}">
        <Setter Property="Margin" Value="{DynamicResource NormalMargion}" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>
</ResourceDictionary>